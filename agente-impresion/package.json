{
  "name": "empremas-agente-impresion",
  "version": "0.1.0",
  "private": true,
  "description": "Agente local que recibe tickets de EMPREMAS por HTTP y los imprime en modo texto (ESC/POS) en una impresora térmica instalada en Windows.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "build": "pkg . --targets node18-win-x64 --output dist/EMPREMAS-Agente-Impresion.exe"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "pkg": "^5.8.1"
  },
  "pkg": {
    "assets": [
      "src/EnviarRaw.ps1"
    ]
  }
}
